# Debian Bookworm Overrides

Environment-specific manifests and configuration for Debian nodes.

Use for staging or environment-specific overrides; apply intentionally and avoid mixing with production folders.
