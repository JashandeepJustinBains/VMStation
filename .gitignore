# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ansible and secrets
.\ansible\group_vars\
ansible/group_vars/
ansible/group_vars/master.yml
ansible/group_vars/all.yml
ansible/group_vars/worker.yml
ansible/group_vars/cloudflared_key.yml
ansible/group_vars/secrets.yml
# Keep template files
!ansible/group_vars/all.yml.template
!ansible/group_vars/secrets.yml.template
secrets/
*.vault
*.key
*.pem
*.env
inventory.yml

# Kubernetes configuration files with credentials
kubeconfig
*.kubeconfig
.kube/

# Temporary files
*.tmp
*.temp
/tmp/
*.log
*.bak
*.backup

# Ansible generated files
*.retry
.ansible/

# Helm charts cache
.helm/
charts/*.tgz

# Container images and archives
*.tar
*.tar.gz
*.tar.bz2
docker-images/
container-images/

# Local development files
.vscode/
.idea/
*.swp
*.swo
*~

# Operating system files
.DS_Store
Thumbs.db
Desktop.ini

# Backup and archive files
/backup/
/archives/
*_backup_*
*.old

# Migration temporary files
/migration_backup/
/legacy_backup/
/podman_backup/

# Build artifacts
dist/
build/
target/
node_modules/
*.pyc
__pycache__/

# Monitoring data
/srv/monitoring_data/
monitoring_data/
prometheus_data/
grafana_data/
loki_data/

# Local registry data
registry_data/
local_registry/

# Development and testing
test_results/
coverage/
.pytest_cache/
.coverage

# Documentation build artifacts
docs/_build/
site/
