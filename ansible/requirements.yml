# Ansible Collections Requirements for VMStation Kubernetes
collections:
  - name: kubernetes.core
    version: ">=2.4.0,<6.0.0"  # Support Ansible 2.14+ through 2.18+
  - name: containers.podman
    version: ">=1.10.0"
  - name: community.general
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"