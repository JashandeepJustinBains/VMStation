---
# =============================================================================
# VMStation Kubernetes Cluster - Main Deployment Orchestration
# Gold-standard, idempotent, never-fail deployment for mixed OS environment
# =============================================================================

- name: Full cluster deployment
  import_playbook: playbooks/deploy-cluster.yaml
