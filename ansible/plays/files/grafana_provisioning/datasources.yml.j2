apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: "{{ prometheus_url | default('http://127.0.0.1:9090') }}"
    isDefault: true
  - name: Loki
    type: loki
    access: proxy
    url: "{{ loki_url | default('http://127.0.0.1:3100') }}"
