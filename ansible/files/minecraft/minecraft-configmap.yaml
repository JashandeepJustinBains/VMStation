apiVersion: v1
kind: ConfigMap
metadata:
  name: minecraft-config
  namespace: default
data:
  server.properties: |
    # Minimal server.properties — edit as needed
    online-mode=true
    motd=VMStation Homelab Minecraft Server
    server-port=25565
    max-players=12
    enable-command-block=false
