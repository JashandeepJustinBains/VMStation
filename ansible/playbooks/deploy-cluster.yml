---
# Cluster deployment playbook: runs all setup roles in order
- name: Prepare and deploy full Kubernetes cluster
  hosts: all
  become: true
  roles:
    - system-prep
    - network-fix
    - cluster-spinup
    - jellyfin
    - monitoring
